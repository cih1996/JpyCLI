# 常用命令

以下命令默认在项目根目录执行。开发调试可用 `go run ./cmd/jpy-cli/main.go`，发布后把 `go run ./cmd/jpy-cli/main.go` 替换为 `jpy` 即可。

## 中间件固件升级

```bash
# 上传本地固件并执行中间件升级
jpy device middleware upgrade --file ./firmware.bin -s 172.25.0.251 -u admin -p 123456

# JSON 输出，方便 UI 或脚本解析
jpy device middleware upgrade --file ./firmware.bin -s 172.25.0.251 -u admin -p 123456 -o json

# 非强制升级
jpy device middleware upgrade --file ./firmware.bin -s 172.25.0.251 -u admin -p 123456 --required=false

# 开发调试写法
go run ./cmd/jpy-cli/main.go device middleware upgrade --file ./firmware.bin -s 172.25.0.251 -u admin -p 123456 -o json
```

## 中间件 ROM 接口刷机

```bash
# 上传 ROM 包
jpy device middleware rom upload --file ./rom.zip -s 172.25.0.251 -u admin -p 123456

# 上传 ROM 包，JSON 输出
jpy device middleware rom upload --file ./rom.zip -s 172.25.0.251 -u admin -p 123456 -o json

# 查看 ROM 包列表，后续 flash 的 --image 通常使用这里返回的 NAME/name
jpy device middleware rom list -s 172.25.0.251 -u admin -p 123456

# 查看 ROM 包列表，JSON 输出
jpy device middleware rom list -s 172.25.0.251 -u admin -p 123456 -o json

# 发起刷机
jpy device middleware rom flash --seat 3 --sn ABC123 --image 1767856234 -s 172.25.0.251 -u admin -p 123456

# 指定刷机模式发起刷机，默认 mode=2
jpy device middleware rom flash --seat 3 --sn ABC123 --image 1767856234 --mode 2 -s 172.25.0.251 -u admin -p 123456 -o json

# 查询全部刷机状态
jpy device middleware rom status -s 172.25.0.251 -u admin -p 123456

# 按盘位查询刷机状态
jpy device middleware rom status -s 172.25.0.251 -u admin -p 123456 --seat 3

# 按 SN 查询刷机状态，JSON 输出
jpy device middleware rom status -s 172.25.0.251 -u admin -p 123456 --sn ABC123 -o json

# 查询刷机详情日志，session 来自 status 返回
jpy device middleware rom detail --seat 3 --session session-id -s 172.25.0.251 -u admin -p 123456

# 查询刷机详情日志，JSON 输出
jpy device middleware rom detail --seat 3 --session session-id -s 172.25.0.251 -u admin -p 123456 -o json
```

## 中间件设备管理

```bash
# 查看设备列表
jpy device list -s 172.25.0.251 -u admin -p 123456

# 查看设备列表，JSON 输出
jpy device list -s 172.25.0.251 -u admin -p 123456 -o json

# 按机位过滤查看设备
jpy device list -s 172.25.0.251 -u admin -p 123456 --seat 3

# 按 IP 过滤查看设备
jpy device list -s 172.25.0.251 -u admin -p 123456 --ip 10.0.0.5

# 按 UUID 过滤查看设备
jpy device list -s 172.25.0.251 -u admin -p 123456 --uuid 7b9f2b7a

# 在指定机位执行命令
jpy device shell "ls /sdcard" -s 172.25.0.251 -u admin -p 123456 --seat 3

# 重启单个中间件下全部设备
jpy device reboot -s 172.25.0.251 -u admin -p 123456

# 重启指定机位
jpy device reboot -s 172.25.0.251 -u admin -p 123456 --seat 3

# 按 IP 过滤重启
jpy device reboot -s 172.25.0.251 -u admin -p 123456 --ip 10.0.0.5

# 按 UUID 过滤重启
jpy device reboot -s 172.25.0.251 -u admin -p 123456 --uuid 7b9f2b7a

# 切换 USB 为 host
jpy device usb -s 172.25.0.251 -u admin -p 123456 --mode host --seat 3

# 切换 USB 为 device
jpy device usb -s 172.25.0.251 -u admin -p 123456 --mode device --seat 3

# 打开 ADB
jpy device adb -s 172.25.0.251 -u admin -p 123456 --set on --seat 3

# 关闭 ADB
jpy device adb -s 172.25.0.251 -u admin -p 123456 --set off --seat 3

# 查看中间件状态
jpy device status -s 172.25.0.251 -u admin -p 123456 -o json
```

## 远程调用

结论：远程调用走 HTTP，不是 WS。被控端先启动 `jpy server`，主控端再用 `--remote` 调用。

支持情况：`device`、`device middleware`、`com`、`flash`、`stress` 都支持全局远程调用；`shell`、`file`、`update`、`version` 有自己的远程写法；`server` 不支持远程执行。

```bash
# 被控端启动 HTTP 服务，默认端口 9090，启动后会一直挂起监听
jpy server --port 9090

# 主控端远程查看设备列表
jpy --remote 10.0.0.5:9090 device list -s 172.25.0.251 -u admin -p 123456

# 远程重启单个中间件下全部设备
jpy --remote 10.0.0.5:9090 device reboot -s 172.25.0.251 -u admin -p 123456

# 远程执行中间件固件升级
jpy --remote 10.0.0.5:9090 device middleware upgrade --file ./firmware.bin -s 172.25.0.251 -u admin -p 123456 -o json

# 远程执行 ROM 接口刷机命令
jpy --remote 10.0.0.5:9090 device middleware rom status -s 172.25.0.251 -u admin -p 123456 -o json

# 远程 COM / flash 命令，适合 COM 口、刷机脚本在被控端的场景
jpy --remote 10.0.0.5:9090 com list
jpy --remote 10.0.0.5:9090 flash run --com COM3 --ch 1 --mw 172.25.0.251 --ip-start 172.25.0.11 --script "C:/flash/002.cmd" -y

# 远程异步调用长任务
jpy --remote 10.0.0.5:9090 flash run --com COM3 --ch 1-20 --mw 172.25.0.251 --ip-start 172.25.0.11 --script "C:/flash/002.cmd" -y --async --async-timeout 0

# shell 是专用远程写法
jpy shell --remote 10.0.0.5:9090 -c "fastboot devices"

# 查询/终止远程异步任务
jpy shell --remote 10.0.0.5:9090 --task <task_id>
jpy shell --remote 10.0.0.5:9090 --kill <task_id>

# file / update / version 使用命令自己的 --remote
jpy file push ./rom.zip --remote 10.0.0.5:9090 --dest D:\flash\rom.zip
jpy update ./jpy-windows-amd64.exe --remote 10.0.0.5:9090
jpy version --remote 10.0.0.5:9090
```

## 批量操作

```bash
# 批量重启多个中间件，middleware.txt 每行一个中间件地址
while read -r MW; do jpy device reboot -s "$MW" -u admin -p 123456; done < middleware.txt

# 并发批量重启多个中间件
cat middleware.txt | xargs -I{} -P 8 sh -c 'jpy device reboot -s "{}" -u admin -p 123456'

# 远程批量重启多个中间件，targets.tsv 每行：REMOTE MW USER PASS
while read -r REMOTE MW USER PASS; do jpy --remote "$REMOTE" device reboot -s "$MW" -u "$USER" -p "$PASS"; done < targets.tsv

# 并发远程批量重启多个中间件
cat targets.tsv | xargs -n 4 -P 6 sh -c 'jpy --remote "$1" device reboot -s "$2" -u "$3" -p "$4"' sh
```

## COM 口刷机方向

```bash
# 上传本地刷机包到远程机器
jpy file push ./rom.zip --remote 192.168.1.100:9090 --dest D:\flash\rom.zip

# 让远程机器从 URL 下载刷机包
jpy file pull "https://example.com/rom.zip" --remote 192.168.1.100:9090 --dest D:\flash\rom.zip

# 单通道刷机
jpy flash run --com COM3 --ch 1 --mw 172.25.0.251 --ip-start 172.25.0.11 --script "C:/ai-services/rom/8se-20260309/002.cmd" -y

# 多通道刷机
jpy flash run --com COM3 --ch 1-10 --mw 172.25.0.251 --ip-start 172.25.0.11 --script "C:/ai-services/rom/8se-20260309/002.cmd" -y

# 指定多个通道刷机
jpy flash run --com COM3 --ch 1,3,5,7 --mw 172.25.0.251 --ip-start 172.25.0.11 --script "C:/ai-services/rom/8se-20260309/002.cmd" -y

# 多 COM 口批量刷机
jpy flash run --com COM3,COM4 --ch 1-20 --mw 172.25.0.251 --ip-start 172.25.0.11 --script "C:/ai-services/rom/8se-20260309/002.cmd" -y

# 所有 COM 口批量刷机
jpy flash run --com all --ch 1-20 --mw 172.25.0.251 --ip-start 172.25.0.11 --script "C:/ai-services/rom/8se-20260309/002.cmd" -y

# 模拟运行，先看任务分配
jpy flash run --com COM3 --ch 1-5 --mw 172.25.0.251 --ip-start 172.25.0.11 --script "C:/ai-services/rom/8se-20260309/002.cmd" --dry

# 指定超时和重试次数
jpy flash run --com COM3 --ch 1-10 --mw 172.25.0.251 --ip-start 172.25.0.11 --script "C:/ai-services/rom/8se-20260309/002.cmd" --timeout 1800 --retry 2 -y

# 远程刷机
jpy --remote 192.168.1.100:9090 flash run --com COM3 --ch 1-10 --mw 172.25.0.251 --ip-start 172.25.0.11 --script "C:/ai-services/rom/8se-20260309/002.cmd" -y

# 远程异步刷机
jpy --remote 192.168.1.100:9090 flash run --com COM3 --ch 1-20 --mw 172.25.0.251 --ip-start 172.25.0.11 --script "C:/ai-services/rom/8se-20260309/002.cmd" -y --async --async-timeout 1800

# 查询远程异步刷机任务
jpy shell --remote 192.168.1.100:9090 --task <task_id>

# 终止远程异步刷机任务
jpy shell --remote 192.168.1.100:9090 --kill <task_id>
```

## COM 控制

```bash
# 列出可用串口
jpy com list -o json

# 查看 COM 通道设备状态
jpy com devices --port COM3 -o json

# 设置单通道为 HUB
jpy com set-mode --port COM3 --mode hub --channel 5

# 设置范围通道为 OTG
jpy com set-mode --port COM3 --mode otg --channel 2-20

# 设置多个通道为 HUB
jpy com set-mode --port COM3 --mode hub --channel 1,2,3

# 重启单通道
jpy com restart --port COM3 --channel 3

# 重启范围通道
jpy com restart --port COM3 --channel 2-20
```

## 远程升级 jpy 程序

```bash
# 远程升级 jpy 程序
jpy update ./jpy-windows-amd64.exe --remote 192.168.1.100:9090

# 从 URL 远程升级 jpy 程序
jpy update https://github.com/xxx/releases/download/v1.5.0/jpy-windows-amd64.exe --remote 192.168.1.100:9090
```

## 压力测试

```bash
# 压力测试，单次
jpy stress user -c zh.json -s "wss://new.jpcloud.vip/ws" -k YOUR_SECRET_KEY

# 压力测试，无限次数改机，测试稳定性
jpy stress user -c zh.json -s "wss://new.jpcloud.vip/ws" -k YOUR_SECRET_KEY --loop 0

# 指定设备列表
jpy stress user -c zh.json -s "wss://new.jpcloud.vip/ws" -k YOUR_SECRET_KEY --device 123,456,789

# 指定循环间隔
jpy stress user -c zh.json -s "wss://new.jpcloud.vip/ws" -k YOUR_SECRET_KEY --loop 3 --interval 5m
```
